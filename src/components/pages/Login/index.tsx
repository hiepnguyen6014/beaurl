import React from "react";

import { LoginContainer, LoginCard } from "./LoginStyled";

const Login: React.FC = () => {
  return (
    <LoginContainer>
      <LoginCard>s</LoginCard>
    </LoginContainer>
  );
};

export default Login;
